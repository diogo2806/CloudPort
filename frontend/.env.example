# URL pública HTTPS do backend consolidado CloudPort.
# Obrigatória no container do frontend quando o configuracao.json empacotado aponta para localhost.
CLOUDPORT_API_URL=https://api.seu-dominio.com

# Opcional: URL pública do Control Room de Navio.
CLOUDPORT_NAVIO_CONTROL_ROOM_URL=
